import styled from "styled-components";
import { containerFlexInCollumn } from "../../../../shared/global/style/mixins";

export const Descricao = styled.span`
    ${containerFlexInCollumn}
    padding: 1rem 0;
    font-family: var(--roboto);
    text-align: center;
    font-size: 16px;
`;